import styled from 'styled-components';

const Wraper = styled.section`
  .percentage-product {
    height: 24px;
    font-size: 12px;
    font-weight: 900;
    padding-left: 10px;
    border-radius: 16px;
    span {
      color: #ff5252;
      align-items: center;
      font-size: 1.25rem;
      display: block;
      height: 100%;
    }
  }
`;

export default Wraper;
